# 백엔드(FastAPI) 전용 환경변수 예시
# 이 파일을 backend/.env 로 복사하여 값 채워 사용하세요.

# 서비스 주소 (리다이렉트/CORS 계산에 사용)
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5174

# 세션 시크릿 (임의의 긴 랜덤 문자열 권장)
SESSION_SECRET=change-this-secret

# AI 서비스 URL (컨테이너 내부 통신용)
AI_SERVICE_URL=http://ai:8600

# MySQL 연결 정보
DB_HOST=project-db-cgi.smhrd.com
DB_PORT=3307
DB_USER=
DB_PASSWORD=
DB_NAME=
# 호환성
DB_PASS=${DB_PASSWORD}

# Kakao OAuth
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=http://localhost:8000/auth/kakao/callback
KAKAO_SCOPE=profile_nickname,profile_image
KAKAO_ADMIN_KEY=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# 기본값은 BACKEND_URL/auth/google/callback 이 사용됩니다.
# GOOGLE_REDIRECT_URI=

# Naver OAuth (선택)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
NAVER_REDIRECT_URI=http://localhost:8000/auth/naver/callback

# Instagram Graph API (개발용) — 장기 사용자 토큰을 서버에만 둡니다.
IG_LONG_LIVED_USER_TOKEN=

# Meta OAuth (Instagram Graph API 로그인용)
META_APP_ID=
META_APP_SECRET=
# 기본값은 BACKEND_URL/oauth/instagram/callback 이 사용됩니다.
META_REDIRECT_URI=
# 최소 권한 예시: 페이지 목록 + 인스타 기본
META_SCOPES=pages_show_list,instagram_basic
