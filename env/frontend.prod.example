# Frontend (Vite) production environment
# Leave empty to use same-origin API via Nginx (recommended)
VITE_API_BASE=
# Optionally define explicit public origin (used by your app if needed)
# PUBLIC_ORIGIN=https://selfstar.duckdns.org
