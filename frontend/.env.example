########################################################
# 프론트엔드(Vite) 전용 환경변수 예시
# 이 파일을 frontend/.env 로 복사하여 값 채워 사용하세요.
# 주의: Vite는 VITE_ 접두사가 붙은 키만 클라이언트에 노출됩니다.
########################################################

# 백엔드 API 베이스 URL
VITE_API_BASE=http://localhost:8000

# (선택) UI에서 카카오 클라이언트ID가 필요하다면 VITE_ 접두사 사용
# VITE_KAKAO_CLIENT_ID=
